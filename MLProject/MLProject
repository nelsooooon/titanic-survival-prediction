name: mlflow-titanic-survival-prediction

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      dataset:
        {
          type: string,
          default: "../res/train_preprocess.csv",
        }
    command: "python modelling.py"
